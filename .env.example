# aplicacao
WEB_PORT=5000
SECRET_KEY=mudar-chave
SCHEDULER_ENABLED=true
SCHEDULER_TIMEZONE=America/Sao_Paulo
RUN_MIGRATIONS=true

# banco de dados usado dentro do Docker
DB_USER=sam
DB_PASSWORD=1234
DB_NAME=samsara_db
DB_HOST=db
DB_PORT=5432

# para usar o sqlite e subir manualmente
# DATABASE_URL=sqlite:///database.db

# email
MAIL_USERNAME=email@profissional
MAIL_PASSWORD=senha-de-app-google
MAIL_DEFAULT_SENDER=email@profissional
